{
  "name": "retire-in-pattaya",
  "type": "module",
  "version": "0.1.0",
  "description": "Retire in Pattaya \u2014 the honest, trustworthy English-language resource for retiring in Pattaya, Thailand.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "node scripts/network-gate.mjs --source",
    "build": "astro build",
    "postbuild": "node scripts/network-gate.mjs --dist",
    "preview": "astro preview",
    "check": "astro check",
    "audit": "node scripts/audit.mjs && node scripts/network-gate.mjs --dist",
    "network:gate": "node scripts/network-gate.mjs"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "@astrojs/sitemap": "3.2.1",
    "@astrojs/mdx": "3.1.9"
  }
}
